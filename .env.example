AWS_ACCESS_KEY_ID=secret
AWS_DEFAULT_REGION=eu-west-1
AWS_SECRET_ACCESS_KEY=secret

CONTAINER_NAME=copilot-cli
CONTAINER_NAME_DIND=dind-copilot-cli

# Auto-approve ALL operations without interactive prompts
# Set to 'true' to enable fully autonomous mode for Copilot CLI
# When enabled, ALL tool operations (git clone, file edits, shell commands, etc.)
# will proceed automatically without requiring user confirmation
# Valid values: true, false, 1, 0, yes, no, on, off
# Default: not set (interactive prompts enabled)
COPILOT_ALLOW_ALL=true

# Github app permissions in Repository permissions: Administration:Read and write / Codespaces metadata: Read-only / Contents: Read and write / Packages: Read and write / Workflows: Read and write
GITHUB_APP_ID=secret
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
secret
-----END RSA PRIVATE KEY-----"
GITHUB_INSTALLATION_ID=secret

PHOENIX_ENDPOINT="https://phoenix.example.com"
PHOENIX_API_KEY="secret"

SSH_PRIVATE_KEY="-----BEGIN OPENSSH PRIVATE KEY-----
secret
-----END OPENSSH PRIVATE KEY-----"

VOLUME_CONFIG=./config
VOLUME_WORKSPACE=./workspace
